import React from 'react';
import ReactDOM from 'react-dom/client';
import BattleSimulator from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <BattleSimulator />
  </React.StrictMode>
);